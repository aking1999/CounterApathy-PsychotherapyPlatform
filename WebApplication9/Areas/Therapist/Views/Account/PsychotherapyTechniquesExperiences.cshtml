@model List<WebApplication9.Areas.Therapist.ViewModels.PsychotherapyTechniqueExperienceViewModel>
@using Framework.Helpers.ExtensionMethods

@{
    ViewData.SetTitle("Iskustva sa psihoterapijskim tehnikama");
    ViewData.SetMetaDescription("Dodajte uže informacije o Vašem iskustvu sa svakom psihoterapijskom tehnikom.");
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section StyleSheets {
    <link rel="stylesheet" href="~/flatpickr/flatpickr.min.css" />
    <link rel="stylesheet" href="~/sweet-alert2/sweetalert2.min.css" />
}

@await Html.TryPartialAsync("~/Views/Shared/Widgets/_NavCard.cshtml",
   new WebApplication9.WidgetViewModels.NavCardWidgetViewModel("far fa-magic fa-sm",
       "text-pink",
       ViewData.GetTitle(),
       ViewData.GetMetaDescription(),
       "col-12",
       "<i class='far fa-star text-warning mr-1'></i>Ove informacije biće prikazane na " +
       $"<a onclick='sessionStorage.setItem(`showSkillsTab`, `true`)' href='{await Url.AuthenticatedPsychotherapistPublicProfileUrlAsync()}'>Vašoj javnoj profilnoj stranici</a>, " +
       "u odeljku \"Struka i veštine\"."))

<div class="row" id="all-psy-tech">
    @if (Model != null && Model.Any())
    {
        foreach (var technique in Model)
        {
            <div id="technique-holder-@technique.Id" class="col-12 col-lg-6">
                <div class="card mb-3">
                    <div class="card-body">
                        <a class="d-block d-flex align-items-center no-tick text-decoration-none"
                           href="#experience-form-@technique.Id"
                           data-toggle="collapse"
                           aria-expanded="false"
                           aria-controls="experience-form-@technique.Id">
                            <i class="@technique.Icon fa-lg fa-fw" style="color: @technique.Color !important"></i>
                            <span class="text-secondary ml-3">@technique.Name</span>
                            <span class="btn btn-mini btn-soft-success rounded-pill border-0 ml-auto">
                                <i class="far fa-plus mr-1"></i>Dodajte iskustvo
                            </span>
                        </a>
                        <div class="collapse" id="experience-form-@technique.Id">
                            <form id="@technique.Id" class="mt-4" autocomplete="off">
                                @Html.AntiForgeryToken()
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-4 text-lg-right">
                                            <label class="fs--1" asp-for="@technique.FromDate"></label>
                                        </div>
                                        <div class="col-lg-7">
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text"><i class="fal fa-calendar fa-fw fa-sm"></i></span>
                                                </div>
                                                <input asp-for="@technique.FromDate"
                                                       class="form-control form-control-sm datetimepicker flatpickr-input"
                                                       placeholder="Datum početka prakse"
                                                       readonly="readonly">
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.FromDate" class="invalid-feedback"></span>
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-4 text-lg-right">
                                            <label class="fs--1" asp-for="@technique.ToDate"></label>
                                        </div>
                                        <div class="col-lg-7">
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text"><i class="fal fa-calendar fa-fw fa-sm"></i></span>
                                                </div>
                                                <input asp-for="@technique.ToDate"
                                                       id="technique_ToDate-@technique.Id"
                                                       class="form-control form-control-sm datetimepicker flatpickr-input"
                                                       placeholder="Datum završetka prakse"
                                                       readonly="readonly">
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.ToDate" class="invalid-feedback"></span>
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-7 offset-lg-4">
                                            <div class="custom-checkbox custom-control custom-control-inline">
                                                @Html.CheckBoxFor(tech => technique.Present, new { @class = "custom-control-input", id = "technique_Present-" + technique.Id })
                                                <label asp-for="@technique.Present"
                                                       for="technique_Present-@technique.Id"
                                                       class="custom-control-label fs--1"
                                                       style="padding-top:0.15rem"></label>
                                                <a href="javascript://"
                                                   class="form-group ml-2 no-tick"
                                                   data-toggle="popover"
                                                   data-placement="bottom"
                                                   data-content="Ukoliko i dalje praktikujete navedenu oblast, <strong>polje za datum završetka prakse ostavite prazno.</strong>"
                                                   data-html="true">
                                                    <i class="fas fa-question-circle fa-sm text-info"></i>
                                                </a>
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.Present" class="invalid-feedback"></span>
                                                <br />
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-4 text-lg-right">
                                            <label class="fs--1" asp-for="@technique.City"></label>
                                        </div>
                                        <div class="col-lg-7">
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text"><i class="fal fa-city fa-fw fa-sm"></i></span>
                                                </div>
                                                <input asp-for="@technique.City" class="form-control form-control-sm">
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.City" class="invalid-feedback"></span>
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-4 text-lg-right">
                                            <label class="fs--1" asp-for="@technique.Country"></label>
                                        </div>
                                        <div class="col-lg-7">
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text"><i class="fal fa-globe-europe fa-fw fa-sm"></i></span>
                                                </div>
                                                <input asp-for="@technique.Country" class="form-control form-control-sm">
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.Country" class="invalid-feedback"></span>
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-lg-4 text-lg-right">
                                            <label class="fs--1" asp-for="@technique.Description"></label>
                                        </div>
                                        <div class="col-lg-7">
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text"><i class="fal fa-align-left fa-fw fa-sm"></i></span>
                                                </div>
                                                <textarea asp-for="@technique.Description"
                                                          class="form-control form-control-sm"
                                                          rows="5"></textarea>
                                            </div>
                                            <span>
                                                <span asp-validation-for="@technique.Description" class="invalid-feedback"></span>
                                                <br />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group mb-0">
                                    <div class="row">
                                        <div class="col-12">
                                            <button data-model-id="@technique.Id"
                                                    class="btn-save-psy-tech btn btn-sm btn-block btn-primary"
                                                    type="button">
                                                <span id="save-text">&nbsp;Sačuvaj&nbsp;</span>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <div class="col-12 text-center px-4 mb-3 text-danger fs-0">
            Trenutno nemate nijednu psihoterapijsku tehniku dodatu na Vaš nalog.
            <br />
            <strong>Ovo je greška jer one moraju biti dodate.</strong>
            <br />
            Molimo kontaktirajte podršku za terapeute i obavestite ih o ovoj grešci.
        </div>
        <div class="col-12 text-center mb-3">
            <a href='@Url.Action("Support", "Account", new { Area = "Therapist" })' class="btn btn-primary">
                <i class="fas fa-headset mr-2"></i>Podrška za terapeute
            </a>
        </div>
    }
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/flatpickr/flatpickr.min.js"></script>
    <script src="~/sweet-alert2/sweetalert2.all.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#container-main main.pb-3').removeClass('pb-3');

            let container = $('#all-psy-tech');
            let save = container.find('.btn-save-psy-tech');

            let techniqueId = sessionStorage.getItem('AddTechniqueExperience');
            if (techniqueId !== null) {
                sessionStorage.removeItem('AddTechniqueExperience');
                let scrollToElement = $(`#technique-holder-${techniqueId}`);
                if (scrollToElement.length) {
                    $(`#experience-form-${techniqueId}`).addClass('show');
                    $('html, body').animate({
                        scrollTop: scrollToElement.offset().top
                    }, 1000);
                }
            }

            $('.datetimepicker').flatpickr({
                dateFormat: "Y-m-d",
            @* altInput: true,
            //altFormat: "j/F/Y",*@
                enableTime: false,
                disableMobile: true,
            @* theme: 'dark',
            defaultDate: new Date(),*@
                onReady: function () {
                    var flatPickrInstance = this;
                    var $flatPickrInput = $(flatPickrInstance.element);
                    $flatPickrInput.siblings("span").click(function () {
                        flatPickrInstance.toggle();
                    });
                }
            });

            save.click(function () {
                let height = $(this).height();
                let modelId = $(this).data('model-id');
                let experienceForm = container.find(`form#${modelId}`);

                DisableInputsInForm(experienceForm, modelId);

                let fromDate = experienceForm.find('#technique_FromDate');
                let toDate = experienceForm.find(`#technique_ToDate-${modelId}`);
                let present = experienceForm.find(`#technique_Present-${modelId}`);
                let city = experienceForm.find('#technique_City');
                let country = experienceForm.find('#technique_Country');
                let description = experienceForm.find('#technique_Description');
                let saveText = experienceForm.find('#save-text');
                let saveTextUnchanged = saveText.html();

                if (present.is(':checked')) {
                    toDate.rules('remove', 'required');
                    let flatpickrToDate = toDate.flatpickr({
                        dateFormat: "Y-m-d",
                        enableTime: false,
                        disableMobile: true,
                    });
                    flatpickrToDate.clear();
                } else {
                    toDate.rules('add', {
                        required: true
                    });
                }

                saveText.fadeOut('fast', function () {
                    $(this).html('<i class="fas fa-spinner fa-spin no-tick"></i>').fadeIn('slow');
                    save.height(height);
                })

                setTimeout(function () {
                    if (FormIsValid(experienceForm, modelId)) {
                        let formData = new FormData(experienceForm.get(0));
                        formData.append('exp.Id', modelId);
                        formData.append('exp.FromDate', fromDate.val());
                        formData.append('exp.ToDate', toDate.val());
                        formData.append('exp.Present', present.is(':checked'));
                        formData.append('exp.City', city.val());
                        formData.append('exp.Country', country.val());
                        formData.append('exp.Description', description.val());

                        $.ajax({
                            url: '@Url.Action("EditPsychotherapyTechniquesExperience", "Account", new { Area = "Therapist" })',
                            type: 'POST',
                            data: formData,
                            processData: false,
                            contentType: false,
                            headers: {
                                'RequestVerificationToken': $("input[name='__RequestVerificationToken']").val()
                            },
                            success: function (response) {
                                if (response.success === true) {
                                    saveText.fadeOut('fast', function () {
                                        $(this).html('<i class="ca-lightgreen fas fa-check no-tick"></i>').fadeIn('slow', function () {
                                            setTimeout(function () {
                                                Swal.fire(
                                                    response.title,
                                                    response.body,
                                                    response.severity
                                                ).then(() => {
                                                    $('a[href="#experience-form-' + modelId + '"]').click();
                                                });

                                                saveText.fadeOut('fast', function () {
                                                    $(this).html(saveTextUnchanged).fadeIn('slow');
                                                    EnableInputsInForm(experienceForm, modelId);
                                                })

                                                if (response.psyTech.present === true) {
                                                    present.prop('checked', true);
                                                    let flatpickrToDate = toDate.flatpickr({
                                                        dateFormat: "Y-m-d",
                                                        enableTime: false,
                                                        disableMobile: true,
                                                    });
                                                    flatpickrToDate.clear();
                                                } else {
                                                    present.prop('checked', false);
                                                }

                                                city.val(response.psyTech.city);
                                                country.val(response.psyTech.country);
                                                description.val(response.psyTech.description);
                                            }, 800)
                                        })
                                    })
                                } else {
                                    if (response.redirectUrl) window.location.href = response.redirectUrl;
                                    else {
                                        saveText.fadeOut('fast', function () {
                                            $(this).html('<i class="text-danger fas fa-times no-tick"></i>').fadeIn('slow');
                                            if (response.showToast) toastr[response.severity](response.body, response.title);
                                            else
                                                Swal.fire(
                                                    response.title,
                                                    response.body,
                                                    response.severity
                                                );
                                        })
                                        .promise()
                                        .done(function () {
                                            setTimeout(function () {
                                                saveText.fadeOut('fast', function () {
                                                    $(this).html(saveTextUnchanged).fadeIn('slow');
                                                    EnableInputsInForm(experienceForm, modelId);
                                                });
                                            }, 1300)
                                        })
                                    }
                                }
                            },
                            error: function (response) {
                                saveText.fadeOut('fast', function () {
                                    $(this).html('<i class="text-danger fas fa-times no-tick"></i>').fadeIn('slow');
                                    toastr['error']('Molimo osvežite stranicu i pokušajte ponovo ili kontaktirajte korisničku podršku.', 'Došlo je do greške');
                                })
                                .promise()
                                .done(function () {
                                    setTimeout(function () {
                                        saveText.fadeOut('fast', function () {
                                            $(this).html(saveTextUnchanged).fadeIn('slow');
                                            EnableInputsInForm(experienceForm, modelId);
                                        });
                                    }, 1300)
                                })
                            }
                        })
                    } else {
                        saveText.fadeOut('fast', function () {
                            $(this).html('<i class="text-danger fas fa-times no-tick"></i>').fadeIn('slow');
                            toastr['info'](null, 'Popunite sva obavezna polja');
                        })
                        .promise()
                        .done(function () {
                            setTimeout(function () {
                                saveText.fadeOut('fast', function () {
                                    $(this).html(saveTextUnchanged).fadeIn('slow');
                                    EnableInputsInForm(experienceForm, modelId);
                                });
                            }, 1300)
                        })
                    }
                }, 1200)
            })

            function ElementInFormIsValid(form, element) {
                return form.validate().element($(element));
            }

            function FormIsValid(form, modelId) {
                let fromDateIsValid = ElementInFormIsValid(form, form.find('#technique_FromDate'));
                let toDateIsValid = ElementInFormIsValid(form, form.find(`#technique_ToDate-${modelId}`));
                let presentIsValid = ElementInFormIsValid(form, form.find(`#technique_Present-${modelId}`));
                let cityIsValid = ElementInFormIsValid(form, form.find('#technique_City'));
                let countryIsValid = ElementInFormIsValid(form, form.find('#technique_Country'));
                let descriptionIsValid = ElementInFormIsValid(form, form.find('#technique_Description'));

                return (fromDateIsValid && toDateIsValid &&
                    presentIsValid && cityIsValid &&
                    countryIsValid && descriptionIsValid);
            }

            function EnableInputsInForm(form, modelId) {
                save.prop('disabled', false);
                form.find(`#technique_Present-${modelId}`).prop('readonly', false);
                form.find('#technique_City').prop('readonly', false);
                form.find('#technique_Country').prop('readonly', false);
                form.find('#technique_Description').prop('readonly', false);
            }

            function DisableInputsInForm(form, modelId) {
                save.prop('disabled', true);
                form.find(`#technique_Present-${modelId}`).prop('readonly', true);
                form.find('#technique_City').prop('readonly', true);
                form.find('#technique_Country').prop('readonly', true);
                form.find('#technique_Description').prop('readonly', true);
            }
        });
    </script>
}