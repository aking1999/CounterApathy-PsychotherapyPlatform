@using Framework.Helpers.ExtensionMethods
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration

<div class="row justify-content-center text-center">
    <div class="col-sm-10 col-md-8 col-lg-6">
        <div class="card">
            <div class="card-body p-4 p-sm-5">
                <h2 class="display-1 text-300 fs-error mb-4"><i class="fas fa-ban text-danger"></i></h2>
                <h4 class="mb-3">Zabranjen pristup zahtevanoj stranici.</h4>
                <hr />
                @if (User.HasSupportAvailable())
                {
                    <p>
                        <a href="@await User.GetSupportUrlAsync()">Kontaktirajte naš tim za podršku</a> ukoliko imate pitanja,
                        ideje ili povratne informacije u
                        vezi sa našim uslugama ovde na @Configuration.GetSection("Application:AppName")?.Value.
                        Rado bismo pomogli u svemu što Vam je potrebno!
                    </p>
                }
                <a asp-area="" asp-controller="Therapists" asp-action="All" class="btn btn-primary btn-block mt-3">
                    <span class="far fa-users mr-2"></span>Pogledajte terapeute
                </a>
            </div>
        </div>
    </div>
</div>