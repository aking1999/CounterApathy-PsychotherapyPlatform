using System.Threading.Tasks;
using Framework.Emails.EmailTypes;
using Framework.Providers;

namespace Framework.Interfaces
{
    public interface IAdminFunctionsProvider
    {
        //Task NotifyAdminsAsync(Database.Models.Notifications notification);
        //Task SendEmailToAdminsAsync(EmailForRole email, UserRoles.UserRoleTypes userRole);
    }
}
